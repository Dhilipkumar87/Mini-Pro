export class Songs{
    constructor(public title: string, public duration: string, public genre: string, public id?: number) {}

    //get title() {
   //     return this._title;
  //  }

   // set title(title: string) {
        // encapsulation checks
    //    this._title = title;
  //  }
}
 